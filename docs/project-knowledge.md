# Project Knowledge — ai-mini-projects

> Generated by knowledge-bootstrap on 2026-04-26. Root `CLAUDE.md` is managed by ai-dev-toolkit (inline rules); this file is the **project** knowledge scaffold. Review and enrich sections marked `<!-- FILL IN -->`.

## Overview

This repo is a **collection of small Python learning/experiment projects** under `mini-projects/`, with shared repo-level tooling (Ruff, pytest discovery) and Cursor commands/rules. `main` integrates projects **01–04**; additional numbered folders (05–09) exist in-tree with per-branch development lines described in `README.md`. Source: [README.md](../README.md).

## Tech Stack

| Category | Technology | Version | Notes |
|----------|-----------|---------|-------|
| Language | Python | 3.11 (target) | `[tool.ruff] target-version = "py311"` in [pyproject.toml](../pyproject.toml) |
| Lint / format | Ruff | (not pinned in root) | `line-length = 100` at repo root |
| Test runner | pytest | (not pinned at root) | `testpaths = ["mini-projects"]` — tests live under each mini-project |
| Package layout | pip + `requirements.txt` | — | Per-project `requirements.txt` under `mini-projects/*` (no root lockfile) |
| Package manager | pip | — | README shows `pip install -r requirements.txt` per project |
| CI | None in repo | — | No `.github/workflows` present |

## Architecture

**Pattern:** Monorepo-style **folder-per-mini-project** under `mini-projects/` (not a Python workspace with shared packages).

**Top-level layout** (repo root):

- `mini-projects/` — numbered projects `01`–`09` (01–04 are the primary integrated set on `main`).
- `pyproject.toml` — shared Ruff + pytest config only.
- `.cursor/` — Cursor commands and rules (partially tracked per README).
- `.claude/` — Claude Code manifest, skills, agents (from ai-dev-toolkit sync).

**Per-project layout** (typical):

- `01-synthetic-data-pipeline`: `pipeline/`, `run_pipeline.py`, `data/`, `requirements.txt`
- `02-resume-job-pipeline`: `pipeline/`, `api/`, `tests/`, `run_pipeline.py`, `data/`
- `03-rag-evaluation-pipeline`: `pipeline/`, `tests/`, `cache/`, `results/`
- `04-rag-pdf-qasystem`: `rag/`, `tests/`, `PLAN.md` (lighter tree)
- `05`–`09`: varying (agents, pipelines, tests) — treat as WIP per README

**Entry points:** Each active project exposes `run_pipeline.py` and/or `api/` modules where applicable; see each `README.md` / `PLAN.md`.

**Data layer:** File-based JSON/JSONL under `data/` in several projects; no shared ORM at repo root.

**Tests:** `tests/` or `testpaths` discovery under `mini-projects/**` per pytest config.

<!-- FILL IN: Any intentional boundaries between mini-projects (shared utils, copy-paste vs import)? -->

## Deployment Pipeline

No automated CI/CD files were found (no `.github/workflows`, `Jenkinsfile`, etc.). Development appears **local-first**: install deps, copy `.env.example` → `.env`, run scripts per README.

<!-- FILL IN: Manual deployment steps, release process, or CI you use outside this repo -->

## Infrastructure

No IaC or cloud config found at repo root (no Terraform, CloudFormation, `docker-compose.yml` in tree scan).

<!-- FILL IN: Cloud accounts, APIs (OpenAI, etc.), secrets handling for mini-projects -->

## Environment Variables

| Variable | Purpose | Where Set |
|----------|---------|-----------|
| (per project) | API keys, endpoints | `.env` — patterns in per-project `.env.example` where present (e.g. `03-rag-evaluation-pipeline`, `01-synthetic-data-pipeline`) |

## Key Conventions

- **Ruff:** Python 3.11 target, line length 100 ([pyproject.toml](../pyproject.toml)).
- **pytest:** Tests collected under `mini-projects/` only.
- **Git:** Feature work on named branches (`02-resume-job-pipeline`, etc.); `main` merges integrated mini-projects per [README.md](../README.md).

<!-- FILL IN: Team conventions not captured in tooling (naming, PR style) -->

## Active Development Areas

- **Recent commits (sample):** ai-dev-toolkit Claude sync; README branch layout; merges of mini-projects 01–04 into `main`.
- **Contributors:** Dominant author in history: Jonathan Boho.
- **Tags:** No release tags observed in recent `git tag` output.
- **Growth areas:** RAG pipelines (03, 04), resume/job synthetic pipeline (02), synthetic data (01); branches 05–09 for future lines.

## Operational Knowledge

<!-- FILL IN: Known issues, gotchas, which branch to use for which experiment, API cost limits -->

## Cross-Project Relationships

- Repo documents dependency on **external APIs** via `.env` (per mini-project); no internal npm/Maven workspace.
- [README.md](../README.md) references **`ai-bootcamp`** only historically (removed in recent commit message).

<!-- FILL IN: Relationship to ai-dev-toolkit, ai-bootcamp, or other repos -->
